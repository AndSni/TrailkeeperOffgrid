# --- Android ---
*.iml
.gradle/
android/local.properties
android/.idea/
android/build/
android/app/build/
android/captures/
android/.cxx/
android/.kotlin/
*.keystore
*.jks
android/keystore.properties

# --- OS / editor ---
.DS_Store
*.swp

# --- Claude Code ---
.claude/settings.local.json
.claude/worktrees/

# NOTE: dist/*.apk is deliberately NOT ignored — release APKs are committed
# so users can install straight from a raw GitHub URL (docs/BLUEPRINT.md 2.3).
