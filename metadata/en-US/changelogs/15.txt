Fixed the "This project" map filter: trails/structures now use a real created-in-project + 10m-proximity rule instead of a loose bounding box, and you can now manually attach an older trail/structure to a project from its detail sheet. Fixed the map staying blank while moving/placing a task or structure, and fixed a moved task's position not refreshing on the map. Task/structure/report map markers are bigger and their size is configurable in Settings > Map. Settings > Offline maps now only lists your downloaded regions plus "Browse all countries" instead of always showing the Baltic quick presets. Added a note from the developer and a Ko-fi support link to Settings > About.
