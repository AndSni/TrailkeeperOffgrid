Split GPX import into dedicated Route/Trail buttons that each import the whole file as that type (fixes "trails only" import failing on most real GPX files). Adding a task or report photo now offers "Take photo" or "Choose from gallery". Task cards got a clearer 3-row layout with urgency-colored status. Added a light theme and 5 accent-color swatches under Settings > Appearance. The task/structure/trail detail sheet now opens fully expanded and scrolls instead of clipping its buttons. Hold-to-delete added for routes and trails.
